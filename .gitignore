# Python
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.egg-info/
*.egg
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment secrets
.env

# Data (downloaded/generated at runtime)
data/raw/
# processed/chunks.json is generated locally by build_chunks.py (58 MB, only
# needed by the ingest step). kanji_table.json is committed — the app needs it
# at runtime and it is small enough (~4 MB) to ship in the Docker image.
data/processed/*
!data/processed/kanji_table.json
data/kanjivg/
data/monitoring/

# Local model weights (cached at runtime)
models/

# IDE & editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-*

# OS
Thumbs.db
.Spotlight-V100
.Trashes

# Project planning notes (kept for context but not committed)
# docs/notes/

# Logs
*.log
logs/

# Streamlit secrets
app/.streamlit/secrets.toml

# UV lock file (platform-specific)
uv.lock
